Este es un buen html, pero esta todo mezclado:

<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Tienda Online. Vainas Artesanales</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
    
    <style>
        /* Estilos base de tus tarjetas */
        .card {
            border-radius: 12px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.05);
            transition: transform 0.2s, box-shadow 0.2s;
            overflow: hidden;
            border: none;
            background: #fff;
            height: 100%;
        }
        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 15px rgba(0,0,0,0.1);
        }
        .img-container {
            height: 200px;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: #f8f9fa;
            overflow: hidden;
        }
        .card-img-top {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
        .blue_button {
            background-color: #3498db;
            color: white;
            text-decoration: none;
            padding: 10px;
            font-weight: 600;
            display: block;
            transition: background 0.2s;
            border: none;
        }
        .blue_button:hover {
            background-color: #2980b9;
            color: white;
        }

        /* Estilos para el Modal de Detalle (Ver Más) */
        .modal-gallery img {
            width: 100%;
            height: 120px;
            object-fit: cover;
            border-radius: 8px;
            cursor: pointer;
            border: 2px solid transparent;
        }
        .modal-gallery img:hover {
            border-color: #3498db;
        }
        .main-modal-img {
            width: 100%;
            height: 350px;
            object-fit: cover;
            border-radius: 12px;
            margin-bottom: 15px;
        }
    </style>
</head>
<body>

    <div class="page-loading active">
        <div class="page-loading-inner">
            <div class="spinner"></div>
            <span>cargando...</span>
        </div>
    </div>
    
    <header>Aquí irá tu Header viejo más adelante</header>

    <div class="super_container">
        <div class="container mt-5 pt-5">
            <div class="row align-items-center">
                <div class="col-lg-12 text-center">
                    <div class="section_title">
                        <img class="img-fluid" src="{{ asset('assets/images/manos212.jpg') }}" alt="portada">
                    </div>
                </div>
            </div>

            <div class="container mt-2 pt-2">
                <div class="row align-items-center">
                    <div class="col-lg-12 text-center">
                        <div class="border-text">
                            <div class="text_overlay"><h3>Nuestros Productos</h3></div>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="row align-items-stretch">
                @foreach($products as $product)
                    <div class="col-6 col-md-3 mt-5 text-center Products">
                        <div class="card"> 
                            
                            <div class="img-container">
                                @if($product->fotos && $product->fotos->foto1)
                                    <img class="card-img-top" src="{{ asset($product->fotos->foto1) }}" alt="{{ $product->nameProd }}">
                                @else
                                    <span class="text-muted">Sin imagen</span>
                                @endif
                            </div>

                            <div class="card-body text-center d-flex flex-column justify-content-between">
                                <h5 class="card-title card_title font-weight-bold">{{ $product->nameProd }}</h5>
                                <div>
                                    <hr>
                                    <p class="card-text p_puntos text-success fw-bold fs-5">
                                        $ {{ number_format($product->precio, 0, '', '.') }}
                                    </p>
                                </div>
                            </div>
                            
                            <button type="button" class="blue_button btn_puntos open-modal-btn" 
                                    data-name="{{ $product->nameProd }}"
                                    data-price="$ {{ number_format($product->precio, 0, '', '.') }}"
                                    data-desc="{{ $product->description_Prod ?? 'Sin descripción disponible.' }}"
                                    data-f1="{{ $product->fotos && $product->fotos->foto1 ? asset($product->fotos->foto1) : '' }}"
                                    data-f2="{{ $product->fotos && $product->fotos->foto2 ? asset($product->fotos->foto2) : '' }}"
                                    data-f3="{{ $product->fotos && $product->fotos->foto3 ? asset($product->fotos->foto3) : '' }}">
                                Ver Más <i class="bi bi-plus-circle"></i>
                            </button>
                        </div>
                    </div>
                @endforeach
            </div>
        </div>

        <footer>Aquí irá tu Footer viejo más adelante</footer>
    </div>

    <div class="modal fade" id="productDetailModal" tabindex="-1" aria-labelledby="modalTitle" aria-hidden="true">
        <div class="modal-dialog modal-lg modal-dialog-centered">
            <div class="modal-content" style="border-radius: 16px;">
                <div class="modal-header border-0">
                    <h4 class="modal-title fw-bold" id="modalTitle">Nombre del Producto</h4>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body p-4">
                    <div class="row">
                        <div class="col-md-6">
                            <img id="modalMainImg" src="" class="main-modal-img" alt="Imagen Principal">
                            <div class="row g-2 modal-gallery">
                                <div class="col-4"><img id="modalThumb1" src="" onclick="changeModalMainImg(this.src)"></div>
                                <div class="col-4"><img id="modalThumb2" src="" onclick="changeModalMainImg(this.src)"></div>
                                <div class="col-4"><img id="modalThumb3" src="" onclick="changeModalMainImg(this.src)"></div>
                            </div>
                        </div>
                        <div class="col-md-6 d-flex flex-direction-column justify-content-between">
                            <div>
                                <h3 id="modalPrice" class="text-success fw-bold mb-3">$ 0</h3>
                                <h5 class="fw-bold">Descripción:</h5>
                                <p id="modalDesc" class="text-muted">Descripción del producto...</p>
                            </div>
                            <div class="mt-4">
                                <button class="btn btn-dark w-100 py-2 fw-bold" style="border-radius: 8px;">Añadir al carrito</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        document.querySelectorAll('.open-modal-btn').forEach(button => {
            button.addEventListener('click', function() {
                // Capturar los datos de la tarjeta desde los atributos data-
                const name = this.getAttribute('data-name');
                const price = this.getAttribute('data-price');
                const desc = this.getAttribute('data-desc');
                const f1 = this.getAttribute('data-f1');
                const f2 = this.getAttribute('data-f2');
                const f3 = this.getAttribute('data-f3');

                // Inyectar datos en el modal
                document.getElementById('modalTitle').innerText = name;
                document.getElementById('modalPrice').innerText = price;
                document.getElementById('modalDesc').innerText = desc;
                
                const mainImg = document.getElementById('modalMainImg');
                mainImg.src = f1 ? f1 : 'placeholder-image-url';

                // Gestionar miniaturas
                setupThumb('modalThumb1', f1);
                setupThumb('modalThumb2', f2);
                setupThumb('modalThumb3', f3);

                // Mostrar el modal
                const myModal = new bootstrap.Modal(document.getElementById('productDetailModal'));
                myModal.show();
            });
        });

        // Auxiliar para ocultar miniaturas vacías
        function setupThumb(id, src) {
            const el = document.getElementById(id);
            if(src) {
                el.src = src;
                el.parentElement.style.display = 'block';
            } else {
                el.parentElement.style.display = 'none';
            }
        }

        // Cambiar imagen grande al hacer clic en miniaturas dentro del modal
        function changeModalMainImg(src) {
            document.getElementById('modalMainImg').src = src;
        }
    </script>
</body>
</html>